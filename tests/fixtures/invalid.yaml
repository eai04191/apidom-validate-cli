openapi: 3.1.0
info:
  title: Sample API
  version: 1.0.0
paths:
  /hello:
    get:
      summary: Say hello
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: notAValidType
                example: deprecated-usage
                properties:
                  message:
                    type: string
